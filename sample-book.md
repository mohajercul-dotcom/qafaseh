---
# ===== فایل نمونه مقاله =====
# این فایل را کپی کن و بگذار در:
#   src/content/books/{fa|ar|ur}/{shiite|atheism|aqeedah|adyan|maqabeh|fraq|daavat}/نام-فایل.md
# هر فیلدی که نیست را حذف کن؛ فقط title و author اجباری هستند.
# آدرس (slug) سایت بهصورت خودکار از روی title ساخته میشود (نام فایل مهم نیست).

# ---------- فیلدهای اجباری ----------
title: "عنوان کامل کتاب"
author: "نام نویسنده"

# ---------- فیلدهای اختیاری ----------

# مترجم
translator: "نام مترجم"

# مشخصات فیزیکی (عدد بدون علامت)
pages: 200
volumes: 1
language: "عربی"

# لینک فایل PDF و فرمت آن
download: "https://example.com/file.pdf"
format: "PDF"

# تصویر جلد؛ اگر حذفش کنی جلد بهصورت خودکار با پسزمینه رنگی ساخته میشود
cover: "/covers/my-book.webp"

# true یعنی کتاب در ویجت «پیشنهادی روز» هم میآید
today: true

# توضیح کوتاه برای ویجت پیشنهادی (کنار عنوان نمایش داده میشود)
caption: "توضیح کوتاه و جذاب برای ویجت پیشنهادی."

# معرفی کوتاه نویسنده — برای هر زبان جداگانه
authorDesc: "توضیح کوتاه درباره نویسنده به فارسی."
authorDescAr: "شرح مختصر عن المؤلف بالعربية."
authorDescUr: "مصنف کے بارے میں مختصر تعارف۔"

# معرفی کتاب — بالای مقاله نمایش داده میشود؛ برای هر زبان جداگانه
description: "معرفی کتاب به فارسی؛ این متن بالای مقاله دیده میشود."
descriptionAr: "تعريف الكتاب بالعربية."
descriptionUr: "کتاب کا تعارف اردو میں۔"

# برچسبها برای فیلتر فهرست کتابها — داخل کوتیشن دوتایی، جدا با کاما
tags: ["رد تشیع", "فضایل صحابه", "کلام"]

# برچسبهایی که فقط روی کارت کتاب دیده میشوند (اگر نبود از tags استفاده میشود)
cardTags: ["فضایل صحابه"]

# برچسب جدا برای فیلتر (اگر نبود از tags استفاده میشود)
filterTags: ["فضایل صحابه"]

# تاریخ انتشار — قالب سال-ماه-روز
pubDate: "2025-03-12"
---

# ===== بدنه مقاله =====

## سرفصل اول

اولین پاراگراف، مقدمه صفحه است و بالای مقاله نمایش داده میشود. پس اولین پاراگراف را معرفی کوتاه و روان بنویس.

**متن پررنگ** و *متن کج* و ~~خطخورده~~.

لیست نامرتب:
- مورد اول
- مورد دوم

لیست مرتب:
1. گام اول
2. گام دوم

## نقلقول

نقلقول با گذاشتن **`>`** در ابتدای خط نوشته میشود. هر پاراگراف نقلقول باید با `>` شروع شود:

> متن نقلقول؛ این جمله داخل کادر با خط رنگی نمایش داده میشود.
> پاراگراف دوم نقلقول هم با > شروع میشود.

برای اضافهکردن نام منبع بعد از نقلقول، در یک خط جدا داخل همان نقلقول، منبع را بین `<cite>` و `</cite>` بنویس:

> متن نقلقول.
> <cite>نام کتاب، جلد ۱، ص ۲۳۵</cite>

(خط منبع به رنگ خاکستری و ریزتر از متن اصلی نمایش داده میشود.)

## لینک

لینک بیرونی:

[متن لینک](https://example.com)

لینک داخلی (به کتاب دیگری در همین سایت):
- اگر این کتاب در پوشه `fa/` است: `[عنوان کتاب دیگر](/books/نام-کتاب)`
- اگر در `ar/` است: `[عنوان کتاب دیگر](/ar/books/نام-کتاب)`
- اگر در `ur/` است: `[عنوان کتاب دیگر](/ur/books/نام-کتاب)`

مثال برای کتاب عربی:

[برای مطالعه بیشتر، کتاب شر الروافض را ببینید](/ar/books/شر-الروافض)

توجه: در آدرس، بهجای فاصله از خط تیره استفاده کن و پرانتز/کاراکترهای خاص را حذف کن (همان قاعدهای که خود سایت برای slug دارد).

## پانویس

عدد پانویس را با این قالب وسط متن میگذاری[^1] و تعریفش را در **انتهای فایل** مینویسی. چند پانویس هم میتوانی داشته باشی؛ مثلاً دومی.[^2]

نکته: تعریف پانویسها همیشه آخر فایل قرار میگیرد:

[^1]: متن پانویس اول، مثلاً: «کتاب فلان، جلد ۱، ص ۲۳۵، چاپ ۱۴۲۵ ه.ق».
[^2]: متن پانویس دوم؛ شمارهها با ترتیب [^1]، [^2]، [^3] و... جلو میروند.
