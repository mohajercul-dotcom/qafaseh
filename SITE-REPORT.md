# گزارش جامع وب‌سایت «قفسه»

> مرجعی تخصصی برای معرفی کتاب‌ها در حوزه اسلام، ادیان و مکاتب فکری؛ با هدف تسهیل پژوهش و دسترسی به منابع معتبر اهل سنت

---

## فهرست مطالب

1. [معرفی کلی](#۱-معرفی-کلی)
2. [محتوا و ساختار سایت](#۲-محتوا-و-ساختار-سایت)
3. [مخاطبان و کاربران هدف](#۳-مخاطبان-و-کاربران-هدف)
4. [ویژگی‌های کاربری](#۴-ویژگی-های-کاربری)
5. [تکنولوژی‌های مورد استفاده](#۵-تکنولوژی-های-مورد-استفاده)
6. [معماری پروژه](#۶-معماری-پروژه)
7. [سرعت و بهینه‌سازی](#۷-سرعت-و-بهینه-سازی)
8. [طراحی رابط کاربری](#۸-طراحی-رابط-کاربری)
9. [سئو](#۹-سئو)
10. [استقرار](#۱۰-استقرار)
11. [جمع‌بندی و مسیرهای آینده](#۱۱-جمع-بندی-و-مسیرهای-آینده)

---

## ۱. معرفی کلی

**قفسه** یک وب‌سایت تخصصی کتاب‌شناسی است که هدف آن معرفی، دسته‌بندی و تسهیل دسترسی به کتاب‌ها و منابع معتبر در حوزه‌های زیر است:

- **اسلام** (عقیده اهل سنت، دعوت، نقد فرقه‌ها و جریان‌ها)
- **ادیان** (نقد و معرفی ادیان غیراسلامی)
- **مکاتب فکری** (مکاتب و جریان‌های فکری، فلسفی و الحادی)

تمرکز اصلی سایت بر **منابع معتبر اهل سنت** است تا پژوهشگران، طلاب، دانشجویان و علاقه‌مندان بتوانند به‌صورت متمرکز به معرفی، مشخصات و لینک دانلود این کتاب‌ها دسترسی داشته باشند.

---

## ۲. محتوا و ساختار سایت

### ۲.۱. دسته‌بندی‌های هفت‌گانه

محتوا در ۷ مجموعه (Collection) مستقل سازماندهی شده است:

| دسته | مسیر | توضیح |
|---|---|---|
| **شیعه** | `/shiite` | معرفی کتاب‌هایی که در نقد و رد باورها و آموزه‌های تشیع نگاشته شده‌اند |
| **الحاد** | `/atheism` | معرفی آثار نقد الحاد و باورهای خداناباورانه |
| **عقیده** | `/aqeedah` | اصول و مبانی اعتقادی اهل سنت (توحید، ایمان، نبوت، معاد و…) |
| **ادیان** | `/adyan` | معرفی و نقد ادیان دیگر |
| **مکاتب فکری** | `/maqabeh` | مکاتب فکری و فلسفی |
| **فرقه‌ها** | `/fraq` | فرقه‌های منتسب به اسلام |
| **دعوت** | `/daavat` | اصول و روش‌های دعوت اسلامی |

### ۲.۲. آمار محتوا

- **تعداد کل کتاب‌ها:** ۳۹ عنوان
  - شیعه: ۲۱
  - الحاد: ۶
  - دعوت: ۴
  - عقیده: ۲
  - ادیان: ۲
  - مکاتب فکری: ۲
  - فرقه‌ها: ۲
- **تعداد نویسندگان:** ۲۶ نویسنده
- **تعداد صفحات ساخته‌شده (در بیلد):** ۱۵۳ صفحه

### ۲.۳. انواع صفحات

1. **صفحه اصلی** (`/`) — هیرو + معرفی دسته‌ها + کتاب پیشنهادی روزانه + کتاب‌های جدید + پربازدیدترین‌ها
2. **صفحات دسته‌بندی** (۷ صفحه) — هر صفحه شامل هیرو با آیکون وکتور، فیلتر تگ‌ها و شبکه‌ی کارت‌های کتاب
3. **صفحه کتاب** (`/books/[slug]`) — متن کامل، مشخصات، دکمه دانلود، ابزار اندازه‌ی فونت، دکمه اشتراک، لینک به دیگر آثار نویسنده
4. **صفحه نویسندگان** (`/authors`) — فهرست نویسندگان به‌همراه شمار آثار در هر موضوع
5. **صفحه نویسنده** (`/authors/[slug]`) — معرفی و فهرست آثار یک نویسنده با فیلتر دسته
6. **صفحه سوالات متداول** (`/faq`) — بیش از ۳۰ پرسش در ۵ گروه
7. **صفحات ۴۰۴ و انتقال** — مسیرهای کوتاه و شناسه برای ریدایرکت به صفحه اصلی کتاب

### ۲.۴. فرانت‌متر و متادیتای هر کتاب

هر کتاب به‌صورت فایل Markdown با فرانت‌متر ساختاریافته نگهداری می‌شود:

- عنوان، نویسنده، مترجم، تعداد صفحات، تعداد جلدها
- زبان، فرمت، لینک دانلود، تصویر جلد
- تعداد بازدید، نشان «امروز» (برای ویجت کتاب پیشنهادی روزانه)
- توضیح کوتاه، توضیح کامل، **تگ‌های موضوعی** (برای فیلتر)
- تاریخ انتشار (برای مرتب‌سازی کتاب‌های جدید)

### ۲.۵. زبان محتوا

محتوا به سه زبان فارسی، عربی و اردو پشتیبانی می‌شود و روی کارت هر کتاب بج زبان نمایش داده می‌شود.

---

## ۳. مخاطبان و کاربران هدف

### ۳.۱. چه کسی دنبال این سایت می‌گردد؟

- **پژوهشگران و محققان علوم اسلامی** که به دنبال معرفی و دسترسی متمرکز به منابع اهل سنت هستند
- **طلاب و دانشجویان دانشگاه‌های علوم اسلامی**
- **دعوت‌گران (داعیان)** که به منابعی در زمینه روش‌های دعوت نیاز دارند
- **محققان نقد فرق، ادیان و الحاد** (نقد تشیع، نقد مسیحیت، نقد الحاد و…)
- **عموم علاقه‌مندان** که به دنبال مطالعه منظم و آگاهانه در این حوزه‌ها هستند

### ۳.۲. کلمات و عبارات کلیدی جستجو

بر اساس دسته‌بندی‌ها و محتوای صفحات، کاربران معمولاً با عباراتی مانند موارد زیر جستجو می‌کنند:

- «کتاب‌های نقد شیعه»، «رد تشیع»
- «کتاب نقد الحاد»، «منابع الحادشناسی»
- «کتاب عقیده اهل سنت»، «معرفی کتاب توحید»
- «نقد ادیان»، «نقد مسیحیت»
- «فرقه‌های اسلامی»، «مکاتب فکری»
- «روش دعوت»، «کتاب دعوت اسلامی»
- «کتاب PDF»، «دانلود کتاب»، «کتاب با ترجمه فارسی»، «کتاب اردو»

### ۳.۳. نقاط قوت از منظر کاربر

- دسترسی **متمرکز** به منابع معتبر در موضوعات پراکنده
- **معرفی کوتاه + متن کامل** برای هر کتاب
- لینک **دانلود** (از کانال تلگرام)
- **جستجو و فیلتر سریع** (توسط تگ و دسته)
- قابلیت **تغییر اندازه فونت** برای مطالعه راحت متن کتاب
- **اشتراک‌گذاری** آسان (کپی لینک، تلگرام، واتساپ، X)
- رابط کاربری **دوزبانه رایانه/موبایل** با تم شب و روز

---

## ۴. ویژگی‌های کاربری

| ویژگی | توضیح | محل پیاده‌سازی |
|---|---|---|
| **جستجوی هوشمند** | جستجوی زنده با نرمال‌سازی کاراکترهای عربی/فارسی/اردو، امتیازدهی و تطبیق تقریبی (فازی / Levenshtein) | `SearchBox.astro` |
| **فیلتر تگ** | نوار چیپ‌های فیلتر با اسکرول افقی و فلش‌های هوشمند؛ روی صفحات دسته و نویسندگان | `TagFilter.astro` |
| **کارت کتاب آکاردئونی** | باز/بسته شدن توضیح کوتاه با کلیک | `BookCard.astro` |
| **کتاب پیشنهادی روزانه** | ویجت چرخشی «امروز» در صفحه اصلی بر اساس فرانت‌متر `today` | `index.astro` |
| **کتاب‌های جدید و پربازدید** | دو بخش در صفحه اصلی | `index.astro` |
| **نمای نویسنده** | صفحه هر نویسنده با شمارش آثار و فیلتر دسته | `authors/[slug].astro` |
| **تغییر اندازه فونت** | سه سایز ۱۵/۱۶/۱۷ با ذخیره در localStorage | `TextSizeToggle.astro` |
| **اشتراک‌گذاری** | کپی لینک + تلگرام + واتساپ + X | `ShareBox.astro` |
| **پاورقی تعاملی** | تولتیپ پاورقی با دکمه کپی، بدون اسکرول به انتهای صفحه | `books/[slug].astro` |
| **تم شب/روز** | ذخیره در localStorage + انیمیشن نرم | `ThemeToggle.astro` |
| **دکمه بازگشت به بالا** | ظاهر شونده بعد از اسکرول، با افکت شیشه‌ای | `MainLayout.astro` |
| **فیلتر دسته در صفحه نویسندگان** | چیپ‌های دسته‌بندی برای فیلتر نویسندگان | `authors.astro` |
| **ریدایرکت هوشمند** | لینک‌های کوتاه/شناسه به صفحه اصلی کتاب | `books/[slug].astro` |
| **مدرال‌ها** | درباره ما، تماس با ما (تلگرام)، انتخاب زبان، منوی دسته‌ها | `Header.astro` |
| **نوار پایین موبایل** | دسترسی سریع: خانه، دسته، تم، زبان | `Header.astro` |
| **تولتیپ‌های راهنما** | توضیح دکمه‌ها با هاور (دسکتاپ) | `Header.astro` |

### ۴.۱. جستجوی هوشمند — جزئیات

جستجو تماماً سمت کلاینت اجرا می‌شود (بدون سرور) و شامل:

- **نرمال‌سازی:** تبدیل یکسان‌ساز کاراکترها (ي→ی، ك→ک، أإآ→ا، ة→ه و…) + تبدیل ارقام عربی/فارسی + حذف اعراب
- **امتیازدهی چندمعیاره:** تطبیق کامل عبارت، تطبیق در عنوان/نویسنده/مترجم/زبان/دسته، تطبیق تقریبی هر توکن (الگوریتم Levenshtein)
- **خروجی رتبه‌بندی‌شده** تا ۷ نتیجه با نمایش دسته، عنوان، نویسنده و بج زبان

### ۴.۲. هشتگ‌ها

هر کارت کتاب تگ‌های موضوعی خود را با پیشوند `#` نمایش می‌دهد (مثلاً «زندگی‌نامه»، «نقد تشیع») که از فرانت‌متر `tags` به‌صورت خودکار تولید می‌شود.

---

## ۵. تکنولوژی‌های مورد استفاده

| بخش | تکنولوژی | نسخه/جزئیات |
|---|---|---|
| **فریم‌ورک** | [Astro](https://astro.build) | `^7.1.6` |
| **زبان‌های برنامه‌نویسی** | TypeScript + JavaScript + SCSS | — |
| **فونت و استایل** | SCSS (Sass) | `^1.102.0` |
| **محتوا** | Content Collections (Glob loader) با Schema Validation (Zod) | `astro:content` |
| **مپ سایت** | `@astrojs/sitemap` | `^3.7.3` |
| **انتقال بین صفحات** | View Transitions API + `<ClientRouter />` | ماژول `astro:transitions` |
| **Node.js** | — | `>=22.12.0` |

### ۵.۱. چرا Astro؟

- **Static Site Generation (SSG):** تمام صفحات در زمان بیلد به HTML استاتیک تبدیل می‌شوند؛ یعنی بهترین سرعت لود ممکن.
- **صفر JavaScript پیش‌فرض:** در Astro هر کامپوننت فقط در صورت نیاز جاوااسکریپت ارسال می‌کند؛ حجم JS نهایی بسیار کم است.
- **Content Collections:** تایپ‌اسکریپت امن با Validation برای فرانت‌متر کتاب‌ها.
- **معماری ایزلند (Islands):** اسکریپت‌های ایزوله و مستقل که بدون تداخل با هم کار می‌کنند.

### ۵.۲. ماژول‌های کمکی پروژه (`src/lib/`)

- `books.js` — توابع `slugify` (ساخت اسلاگ از عنوان)، `normalizeAuthor`، `shortCode` (کد کوتاه از هش djb2)
- `number.ts` — تبدیل ارقام به فارسی (`toPersian`)
- `bookGrid.js` — چیدمان چندستونه‌ی کارت‌ها (ماسونی) بر اساس عرض صفحه
- `bookFilter.js` — فیلتر کارت‌ها بر اساس تگ/دسته
- `buildTagFilters.js` — ساخت خودکار فهرست تگ‌ها با مرتب‌سازی بر اساس فراوانی

---

## ۶. معماری پروژه

### ۶.۱. ساختار پوشه‌ها

```
src/
├── layouts/MainLayout.astro   # قالب اصلی (هدر + بدنه + دکمه بازگشت به بالا)
├── components/                # کامپوننت‌های Astro
│   ├── Header.astro           # هدر دسکتاپ + موبایل + مدرال‌ها
│   ├── SearchBox.astro        # جستجوی زنده
│   ├── BookCard.astro         # کارت کتاب آکاردئونی
│   ├── TagFilter.astro        # نوار فیلتر تگ
│   ├── BookCover.astro        # جلد کتاب (تصویر یا وکتور)
│   ├── ShareButton.astro      # دکمه اشتراک
│   ├── ShareBox.astro         # پنل اشتراک (کپی + تلگرام + واتساپ + X)
│   ├── ThemeToggle.astro      # تم شب/روز
│   ├── TextSizeToggle.astro   # تغییر اندازه فونت
│   ├── AboutPopup.astro / ContactPopup.astro
│   ├── Logo.astro / IconButton.astro
│   └── icons/                 # آیکون‌های وکتور (SVG)
├── content/
│   ├── content.config.ts      # تعریف ۷ کالکشن + اسکیمای Zod
│   └── books/{7 دسته}/        # فایل‌های Markdown کتاب‌ها
├── lib/                       # توابع کمکی
├── pages/                     # صفحات (فول‌استاتیک + داینامیک)
│   ├── index.astro
│   ├── {shiite,a...}.astro    # ۷ صفحه دسته
│   ├── authors.astro + authors/[slug].astro
│   ├── books.astro + books/[slug].astro
│   └── faq.astro
└── styles/global.scss         # استایل سراسری، متغیرهای تم، view-transition
```

### ۶.۲. سیستم اسلاگ و URL

- اسلاگ هر کتاب از **عنوان فارسی** ساخته می‌شود (حروف عربی/فارسی + اعداد پشتیبانی می‌شوند)
- هر کتاب **سه URL** دارد که دو تای آن به‌صورت متا-رفرش به URL اصلی ریدایرکت می‌شوند:
  - `/books/{عنوان-کتاب}` (اصلی)
  - `/books/{کد-کوتاه}` (برای اشتراک‌گذاری)
  - `/books/{شناسه-فایل}`
- لینک‌های اشتراک از **کد کوتاه** استفاده می‌کنند تا URL کوتاه و تمیز بماند

### ۶.۳. مدیریت اسکریپت‌ها با View Transitions

چون صفحات بین صفحه‌ای منتقل می‌شوند و DOM عوض می‌شود، همه‌ی اسکریپت‌های تعاملی از یک الگوی **re-init** پیروی می‌کنند:

- شنونده روی رویداد `astro:page-load` (پس از هر ناوبری)
- گارد `dataset.bound` برای جلوگیری از دوبار بایند شدن
- گاردهای سراسری `window.__*Bound` برای شنونده‌های سند/پنجره

این الگو تضمین می‌کند که جستجو، آکاردئون‌ها، فیلترها و مدرال‌ها بعد از هر انتقال بین صفحات درست کار کنند.

---

## ۷. سرعت و بهینه‌سازی

### ۷.۱. بهینه‌سازی‌های اصلی

1. **تولید استاتیک کامل (SSG):** هیچ درخواست سمت سرور در هنگام بازدید وجود ندارد.
2. **پیش‌بارگذاری فونت‌ها:** سه وزن قلم Vazirmatn با `<link rel="preload">` و `font-display:swap`.
3. **فونت وواف۲:** فونت‌ها به فرمت woff2 (فشرده‌ترین) ارائه شده‌اند.
4. **SVG به جای PNG:** آیکون‌ها و لوگوها وکتور (SVG) هستند — حجم ناچیز و بدون افت کیفیت.
5. **ClientRouter / View Transitions:** ناوبری بدون رفرش کامل؛ فقط HTML جدید واکشی می‌شود، بنابراین سرعت جابجایی بین صفحات زیاد است.
6. **اسکریپت‌های ایزوله:** جاوااسکریپت فقط برای اجزای تعاملی ارسال می‌شود.
7. **تصاویر جلد به‌صورت فایل استاتیک** در `public/covers`.
8. **بدون کتابخانه UI سنگین:** همه‌چیز با CSS/SCSS دستی و بدون وابستگی‌های اضافه.

### ۷.۲. وابستگی‌های نصب‌شده (سبک و کم‌حجم)

تنها دو پکیج runtime دارد:
- `astro` (فریم‌ورک اصلی)
- `@astrojs/sitemap` (تولید نقشه سایت)

و یک پکیج devDependency:
- `sass` (کامپایل SCSS)

یعنی **حجم پکیج‌ها و جاوااسکریپت ارسالی به مرورگر حداقل است**.

### ۷.۳. مقیاس بیلد

- ۱۵۳ صفحه در حدود **۱ ثانیه** ساخته می‌شود.
- افزایش کتاب‌ها (فایل Markdown) به‌صورت خودکار صفحات جدید تولید می‌کند.

---

## ۸. طراحی رابط کاربری

### ۸.۱. جهت و زبان

- زبان فارسی با جهت **RTL** (`dir="rtl"`)
- تایپوگرافی **وزیرمتن** (Vazirmatn) با سه وزن 400/500/700
- پشتیبانی از اعداد فارسی (تابع `toPersian`)

### ۸.۲. تم شب و روز

- متغیرهای CSS در `:root` و `.dark` (پس‌زمینه کرم روشن / قهوه‌ای تیره)
- رنگ تاکیدی (Accent) قهوه‌ای-طلایی (`#B0651F` روشن / `#E0A458` تیره)
- ذخیره تم در `localStorage` و اعمال پیش از رندر (بدون فلش)
- انیمیشن نرم تغییر تم (`theme-anim`)

### ۸.۳. View Transitions

- ناوبری با اسلاید افقی (مطابق جهت RTL) با مدت ~۰.۴۲ ثانیه
- هدر با `view-transition-name` مستقل انیمیت می‌شود
- پشتیبانی از `prefers-reduced-motion`

### ۸.۴. واکنش‌گرایی (Responsive)

- **دسکتاپ (۹۰۱px به بالا):** منوی افقی کامل، هدر ۸۰ پیکسلی
- **موبایل (۹۰۰px به پایین):** منوی بالا + نوار پایین (خانه، دسته، تم، زبان)، جستجوی موبایل، پاپ‌آپ دسته‌ها/بیشتر
- **قرینه/تبلت:** چیدمان ۲ یا ۳ ستونه کتاب‌ها (ماسونی)
- کد شکستن `900px` برای ناوبری و `1000px` برای صفحه کتاب

### ۸.۵. آیکون‌ها و لوگو

- هیروی هر صفحه دسته یک **آیکون وکتور** مخصوص دارد (شیعه، الحاد، عقیده، ادیان، مکاتب، فرق، دعوت، نویسندگان، FAQ) با نسخه روشن/تیره
- لوگوی اصلی سایت از آیکون SVG (تولید شده در پروژه) استفاده می‌کند

---

## ۹. سئو

- **Sitemap.xml** با `@astrojs/sitemap` با تنظیمات دقیق:
  - صفحه اصلی: اولویت ۱.۰، روزانه
  - صفحات دسته‌بندی (شیعه، الحاد): اولویت ۰.۹، هفتگی
  - صفحات کتاب: اولویت ۰.۸، هفتگی
  - صفحات نویسندگان: اولویت ۰.۶، ماهانه
- **robots.txt** برای راهنمایی خزنده‌ها
- **متاتگ description** در قالب اصلی
- **URLهای معنادار** فارسی (اسلاگ از عنوان کتاب)
- **HTML استاتیک** برای ایندکس‌گذاری سریع توسط موتورهای جستجو
- آدرس سایت کانونیکال: `https://qafaseh.vercel.app`
- صفحه اصلی حاوی **کلمات کلیدی هدف** (مرجع تخصصی، معرفی کتاب، اسلام، ادیان، مکاتب فکری، اهل سنت)

---

## ۱۰. استقرار

- **هاست:** Vercel (در پیکربندی: `site: 'https://qafaseh.vercel.app'`)
- **مدیریت ریپو:** GitHub (`mohajercul-dotcom/qafaseh`) روی شاخه `master`
- **فرآیند:** استاتیک بیلد با دستور `npm run build` و استقرار خودکار

---

## ۱۱. جمع‌بندی و مسیرهای آینده

### ۱۱.۱. خلاصه نقاط قوت

- ✅ معرفی متمرکز و تخصصی منابع معتبر اهل سنت در ۷ حوزه
- ✅ سرعت بارگذاری بسیار بالا (تولید استاتیک + فونت‌های سبک + بدون JS اضافه)
- ✅ ناوبری روان بدون رفرش با View Transitions
- ✅ جستجوی هوشمند مقاوم به اشتباهات تایپی و کاراکترهای عربی/فارسی/اردو
- ✅ تجربه کاربری کامل موبایل (نوار پایین، پاپ‌آپ‌ها، فیلترها)
- ✅ دسترس‌پذیری برای مطالعه (اندازه فونت قابل تغییر، تم شب)

### ۱۱.۲. مسیرهای پیشنهادی

- افزودن فیلتر بر اساس زبان و نویسنده در صفحه دسته‌ها
- صفحه‌بندی (Pagination) برای دسته‌های پرکتاب
- ریچ اسنیپت‌های Schema.org (Book/Author) برای نمایش بهتر در گوگل
- Open Graph / توییتر کارت برای نمایش زیبای لینک‌ها در شبکه‌های اجتماعی
- آمار بازدید و تحلیل رفتار کاربر (Analytics سبک)
- نسخه عربی و اردوی کامل رابط کاربری (فراتر از محتوا)

---

> گزارش تهیه‌شده بر اساس وضعیت فعلی کد و محتوای مخزن در آگوست ۲۰۲۶.
